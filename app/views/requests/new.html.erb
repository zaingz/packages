<%= form_for @request do |f| %>
    <%= params.inspect%>
    <br>

    <% if @request.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(@request.errors.count, "error") %> prohibited this package from being saved:</h2>

          <ul>
            <% @request.errors.full_messages.each do |message| %>
                <li><%= message %></li>
            <% end %>
          </ul>
        </div>
    <% end %>

    <%= f.label :message%>
    <%=f.text_area :message %>
    <%= f.hidden_field :package_id, value: params[:package_id]%>


    <%= params.inspect%>
    <%= f.submit %>

<%end%>